jar {
    manifest {
        attributes([
                'Maven-Artifact': "${project.group}:${project.archivesBaseName}:${project.version}:utilities",
                'Timestamp': System.currentTimeMillis()
        ])
    }

    classifier 'utilities'
}
