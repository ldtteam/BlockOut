jar {
    manifest {
        attributes([
                'Maven-Artifact': "${project.group}:${project.archivesBaseName}:${project.version}:guidebook",
                'Timestamp': System.currentTimeMillis(),
                'FML_AT': 'blockout_at.cfg'
        ])
    }

    classifier 'guidebook'
}
